/* FramePro Mobile Sensors V25 — giroscópio estável, sem retorno de bússola */
(function(){
'use strict';
const mobile=/Android|iPhone|iPad|iPod|Mobile/i.test(navigator.userAgent||'')||(window.matchMedia&&matchMedia('(pointer:coarse)').matches);if(!mobile)return;
const html=document.documentElement;
const cameraOffset=window.__fpMobileCameraOffset=window.__fpMobileCameraOffset||{x:0,y:0,roll:0};
const state=window.__fpSensorRuntimeV25=window.__fpSensorRuntimeV25||{enabled:false,paused:false,centering:false,samples:0,gyroSamples:0,lastAction:0,lastGyroAt:0,yawRate:0,pitchRate:0,orientation:null,rollBase:null,appliedRoll:0,fallbackLast:null};
if(!document.getElementById('fp-sensor-v25-style')){const s=document.createElement('style');s.id='fp-sensor-v25-style';s.textContent=`#fpCenterV4,#fpSensorV4{pointer-events:auto!important;visibility:visible!important}#fpCenterV4{display:grid!important}html.fp-sensor-on #fpLookV4,#fpLookV4{pointer-events:auto!important;opacity:1!important;visibility:visible!important}`;document.head.appendChild(s);}
function rawLook(dx,dy){if(typeof window.__fpMobileRawLook==='function'){window.__fpMobileRawLook(dx,dy);return;}if(typeof window.__fpApplyLook==='function'){window.__fpApplyLook(dx,dy);return;}const target=document.querySelector('#gameShell canvas')||document.querySelector('canvas')||document;const ev=new MouseEvent('mousemove',{bubbles:true,cancelable:true,view:window});try{Object.defineProperties(ev,{movementX:{value:dx},movementY:{value:dy}});}catch(_){}target.dispatchEvent(ev);document.dispatchEvent(ev);window.dispatchEvent(ev);}
function trackedLook(dx,dy){if(!Number.isFinite(dx)||!Number.isFinite(dy))return;if(typeof window.__fpMobileTrackedLook==='function'){window.__fpMobileTrackedLook(dx,dy);return;}rawLook(dx,dy);cameraOffset.x+=dx;cameraOffset.y+=dy;}
function wheel(delta){(document.querySelector('#gameShell canvas')||document.body).dispatchEvent(new WheelEvent('wheel',{deltaY:delta,bubbles:true,cancelable:true}));}
function norm(v){while(v>180)v-=360;while(v<-180)v+=360;return v;}
function clamp(v,a,b){return Math.max(a,Math.min(b,v));}
function screenAngle(){return (((screen.orientation&&screen.orientation.angle)||window.orientation||0)%360+360)%360;}
function toast(text){let t=document.getElementById('fpSensorToastV25');if(!t){t=document.createElement('div');t.id='fpSensorToastV25';Object.assign(t.style,{position:'fixed',left:'50%',top:'72px',transform:'translateX(-50%)',zIndex:'300000',padding:'8px 12px',borderRadius:'10px',background:'rgba(5,12,20,.94)',color:'#fff',font:'800 11px system-ui',pointerEvents:'none',opacity:'0',transition:'opacity .2s'});document.body.appendChild(t);}t.textContent=text;t.style.opacity='1';clearTimeout(t.__timer);t.__timer=setTimeout(()=>t.style.opacity='0',1500);}
function mapOrientation(e){if(!Number.isFinite(e.alpha)&&!Number.isFinite(e.beta)&&!Number.isFinite(e.gamma))return null;const a=Number(e.alpha)||0,b=Number(e.beta)||0,g=Number(e.gamma)||0,angle=screenAngle();if(angle===90)return{yaw:a,pitch:g,roll:b-90};if(angle===270)return{yaw:a,pitch:-g,roll:-(b+90)};if(angle===180)return{yaw:a,pitch:-b,roll:-g};return{yaw:a,pitch:b,roll:g};}
function mapGyro(r){if(!r)return null;const a=Number(r.alpha)||0,b=Number(r.beta)||0,g=Number(r.gamma)||0,angle=screenAngle();if(angle===90)return{yaw:-b,pitch:g,roll:a};if(angle===270)return{yaw:b,pitch:-g,roll:a};if(angle===180)return{yaw:-g,pitch:-b,roll:a};return{yaw:g,pitch:b,roll:a};}
function recalibrate(){state.fallbackLast=state.orientation?{...state.orientation}:null;state.rollBase=state.orientation?state.orientation.roll:null;state.yawRate=0;state.pitchRate=0;}
function updateRoll(cur){if(!cur)return;if(state.rollBase===null)state.rollBase=cur.roll;const rel=norm(cur.roll-state.rollBase);let wanted=state.appliedRoll;if(Math.abs(rel)<2.8)wanted=0;else{const candidate=clamp(Math.round(rel/5.5),-4,4);if(Math.abs(candidate-state.appliedRoll)>=1&&Math.abs(rel)>Math.abs(state.appliedRoll)*5.5+1.2)wanted=candidate;}if(wanted!==state.appliedRoll){const dir=Math.sign(wanted-state.appliedRoll);wheel(dir>0?11:-11);state.appliedRoll+=dir;cameraOffset.roll=state.appliedRoll;}}
function onOrientation(e){if(!state.enabled||state.paused||document.hidden)return;const cur=mapOrientation(e);if(!cur)return;state.orientation=cur;state.samples++;updateRoll(cur);if(performance.now()-state.lastGyroAt<900||state.centering||window.__fpJoystickLookActive)return;if(!state.fallbackLast){state.fallbackLast={...cur};return;}let dyaw=norm(cur.yaw-state.fallbackLast.yaw),dpitch=cur.pitch-state.fallbackLast.pitch;state.fallbackLast={...cur};if(Math.abs(dyaw)>4||Math.abs(dpitch)>4)return;dyaw=clamp(dyaw,-1.6,1.6);dpitch=clamp(dpitch,-1.3,1.3);if(Math.abs(dyaw)>.08)trackedLook(-dyaw*.85,0);if(Math.abs(dpitch)>.08)trackedLook(0,dpitch*.72);}
function onMotion(e){if(!state.enabled||state.paused||state.centering||document.hidden||window.__fpJoystickLookActive)return;const g=mapGyro(e.rotationRate);if(!g)return;state.gyroSamples++;state.lastGyroAt=performance.now();let dt=Number(e.interval)/1000;if(!Number.isFinite(dt)||dt<=0||dt>.08)dt=.016;let yaw=clamp(g.yaw,-150,150),pitch=clamp(g.pitch,-120,120);if(Math.abs(yaw)<1.1)yaw=0;if(Math.abs(pitch)<1.1)pitch=0;state.yawRate=state.yawRate*.58+yaw*.42;state.pitchRate=state.pitchRate*.62+pitch*.38;if(!yaw)state.yawRate*=.55;if(!pitch)state.pitchRate*=.55;const dx=clamp(-state.yawRate*dt*1.22,-3.2,3.2),dy=clamp(state.pitchRate*dt*.94,-2.5,2.5);if(Math.abs(dx)>.012)trackedLook(dx,0);if(Math.abs(dy)>.012)trackedLook(0,dy);}
async function permission(){if(typeof DeviceOrientationEvent==='undefined'&&typeof DeviceMotionEvent==='undefined')throw new Error('sem suporte');if(typeof DeviceOrientationEvent!=='undefined'&&typeof DeviceOrientationEvent.requestPermission==='function'){const p=await DeviceOrientationEvent.requestPermission();if(p!=='granted')throw new Error('negado');}if(typeof DeviceMotionEvent!=='undefined'&&typeof DeviceMotionEvent.requestPermission==='function'){const p=await DeviceMotionEvent.requestPermission();if(p!=='granted')throw new Error('negado');}}
function unbind(){window.removeEventListener('deviceorientation',onOrientation,true);window.removeEventListener('devicemotion',onMotion,true);}
function bind(){unbind();if(state.enabled&&!state.paused){window.addEventListener('deviceorientation',onOrientation,true);window.addEventListener('devicemotion',onMotion,true);}}
function refresh(){const b=document.getElementById('fpSensorV4');if(b)b.textContent=state.enabled?'📱 SENSORES ON':'📱 ATIVAR SENSORES';const c=document.getElementById('fpCenterV4');if(c){c.textContent='⟳ CENTRALIZAR VISÃO';c.style.setProperty('display','grid','important');}html.classList.toggle('fp-sensor-on',state.enabled);}
async function toggleSensors(){try{await permission();state.enabled=!state.enabled;state.samples=state.gyroSamples=0;state.lastGyroAt=0;state.orientation=null;state.rollBase=null;state.appliedRoll=0;cameraOffset.roll=0;recalibrate();bind();refresh();if(state.enabled){toast('Giroscópio ativado e calibrado');setTimeout(()=>{if(state.enabled&&state.gyroSamples===0)toast('Usando modo compatível do sensor');},2200);}else toast('Sensores desligados');}catch(_){state.enabled=false;unbind();refresh();const b=document.getElementById('fpSensorV4');if(b)b.textContent='⚠️ SENSOR INDISPONÍVEL';toast('Permissão ou sensor indisponível');}}
function centerLevel(){if(state.centering)return;state.centering=true;if(typeof window.__fpResetMobileControls==='function')window.__fpResetMobileControls();const pitch=Number(cameraOffset.y)||0,roll=Number(state.appliedRoll)||0,pitchSteps=Math.abs(pitch)>.01?14:0,rollSteps=Math.abs(Math.round(roll)),total=Math.max(pitchSteps,rollSteps,1);let i=0;function finish(){cameraOffset.y=0;cameraOffset.roll=0;state.appliedRoll=0;state.centering=false;recalibrate();toast('Visão vertical e horizonte nivelados');}function tick(){if(pitchSteps&&i<pitchSteps)rawLook(0,-pitch/pitchSteps);if(i<rollSteps)wheel(roll>0?-11:11);i++;if(i<total)requestAnimationFrame(tick);else finish();}requestAnimationFrame(tick);}
function actionFromEvent(e){const target=e.target&&e.target.closest?e.target.closest('#fpSensorV4,#fpCenterV4'):null;if(!target)return;const now=performance.now();if(now-state.lastAction<350)return;state.lastAction=now;e.preventDefault();e.stopPropagation();if(target.id==='fpSensorV4')toggleSensors();else centerLevel();}
document.addEventListener('pointerup',actionFromEvent,true);document.addEventListener('click',actionFromEvent,true);
function suspend(){state.paused=true;state.yawRate=state.pitchRate=0;unbind();if(typeof window.__fpResetMobileControls==='function')window.__fpResetMobileControls();}
function resume(){state.paused=false;state.lastGyroAt=0;state.yawRate=state.pitchRate=0;recalibrate();setTimeout(()=>{bind();refresh();},120);setTimeout(()=>{bind();refresh();},650);}
window.addEventListener('pagehide',suspend,true);window.addEventListener('blur',suspend,true);document.addEventListener('visibilitychange',()=>document.hidden?suspend():resume(),true);window.addEventListener('pageshow',resume,true);window.addEventListener('focus',()=>{if(!document.hidden)resume();},true);window.addEventListener('orientationchange',()=>{state.orientation=null;state.rollBase=null;resume();},true);
window.__fpRecenterMobile=centerLevel;window.__fpRecalibrateSensorsOnly=recalibrate;window.__fpReinstallSensors=()=>{bind();refresh();};window.__fpSensorState={get enabled(){return state.enabled;},toast,recalibrateOnly:recalibrate};
bind();refresh();setInterval(refresh,900);
})();